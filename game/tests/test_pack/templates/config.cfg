[test_template.png]

textboxes = [
    {}, # Default values.
    { "x": 10, "y": 10, "w": 200, "h": 200, "lines": 5 },
    { "x": -20, "y": -40, "w": 0, "h": 0, "text": "urm...
... this is awkward." }, # Invalid rectangle.
    { "x": 100, "y": 100, "w": 400, "h": 100, "rot": 90, "text": "hi" },
    { "w": 1000, "h": 50, "lines": 100 }, # Over maximum number of lines.
]

[old_template.png]

# This template uses the old 'textboxes' dictionary format, where the keys were
# the IDs of the textboxes.
textboxes = {
    "blank": {},
    "filled": { "x": 200, "y": 100, "w": 100, "h": 20, "rot": 180 }
}
